<UserControl x:Class="WindowsClient.Views.WelcomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"

             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Background="Black">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Image Grid.Column="0" 
               Source="/Resourses/Img/welcome-image.png" 
               
               HorizontalAlignment="Left"
               VerticalAlignment="Stretch"
               />

        <StackPanel Grid.Column="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    >
            <TextBlock Text="Welcome to&#x0a;post quantum&#x0a;end-to-end encryption&#x0a;messenger"
                       Foreground="White"
                       FontSize="40"
                       FontWeight="Bold"
                       TextWrapping="Wrap"
                       TextAlignment="Left" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left"  Margin="0,20,0,0" >
                <Button Style="{DynamicResource YellowLgButton}" Margin="0,0,15,0" Content="Create account"
                        Click="CreateAccount_Click" />
                
                <Button Style="{DynamicResource WhiteLgButton}" Content="Sign In"
                        Click="SignIn_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
